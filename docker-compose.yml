# docker-compose.yml
services:
  jekyll:
    image: jekyll/builder:latest
    volumes:
      - .:/srv/jekyll
      - ./_site:/srv/jekyll/_site
    command: jekyll build --future
    environment:
      - JEKYLL_ENV=production
    user: "1001:127"  # Match GitHub Actions runner user